#include <stdio.h>

/* Auto-generated: solution with only necessary headers included */


int main() {
    double a,b;
    if(scanf("%lf %lf", &a,&b)!=2) return 0;
    printf("Sum = %.6g\n", a+b);
    return 0;
}
